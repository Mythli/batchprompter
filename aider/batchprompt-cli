#!/bin/bash
PATHS="apps/batchprompt-cli/src"
if [ -d "apps/batchprompt-cli/tests" ]; then
    PATHS="$PATHS apps/batchprompt-cli/tests"
fi

aider $(find $PATHS -type d -name "generated" -prune -o -type f \( -name "*.ts" -o -name "*.js" \) ! -name "*.d.ts" -print) "$@"
