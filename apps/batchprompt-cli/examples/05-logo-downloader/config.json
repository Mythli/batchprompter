{
  "concurrency": 2,
  "output": {
    "tmpDir": "out/05-logo-downloader/.tmp"
  },
  "dataOutputPath": "out/05-logo-downloader/results.csv",
  "steps": [
    {
      "plugins": [
        {
          "type": "logoScraper",
          "url": "{{website_url}}",
          "output": {
            "mode": "column",
            "column": "brand_assets"
          }
        }
      ],
      "model": {
        "prompt": "Describe the brand identity of {{website_url}} based on the extracted logo and colors."
      },
      "output": {
        "mode": "column",
        "column": "brand_description"
      }
    }
  ]
}
